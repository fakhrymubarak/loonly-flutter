import 'package:flutter/material.dart';

const spacingLarger = 24.0;
const spacingLarge = 20.0;
const spacingRegular = 16.0;
const spacingSmall = 10.0;
const spacingSmaller = 8.0;
const spacingTiny = 4.0;

const fontLarge = 16.0;
const fontRegular = 12.0;
const fontMedium = 10.0;
const fontSmall = 10.0;

const radCircle = BorderRadius.all(Radius.circular(100.0));
const radRegular = BorderRadius.all(Radius.circular(10.0));
const radSmall = BorderRadius.all(Radius.circular(8.0));
const radSmaller = BorderRadius.all(Radius.circular(6.0));
const radTiny = BorderRadius.all(Radius.circular(4.0));
